import { SearchStudentsPipe } from './search-students-pipe';

describe('SearchStudentsPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchStudentsPipe();
    expect(pipe).toBeTruthy();
  });
});
